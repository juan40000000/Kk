{
  "name": "kk-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=meta-ads-simulator",
    "build": "npm run build --workspace=meta-ads-simulator",
    "test": "npm run test --workspace=meta-ads-simulator"
  }
}
